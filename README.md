#Content URL Rewriter plugin#
##WHat is it?##
This plugin rewrites the URL's in your database. It does this afterwards, so the actual content
will not be changed. This is done to avoid screwing up your actual content and to allow 
more easily move data from dev to test and staging

##Installation##
Use composer and install it as a plugin or mu-plugin

